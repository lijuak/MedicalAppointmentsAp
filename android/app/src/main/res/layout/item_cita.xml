<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/cardCita"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginBottom="12dp"
    app:cardCornerRadius="20dp"
    app:cardElevation="5dp"
    app:strokeWidth="1dp"
    app:strokeColor="#FFE0CC"
    android:foreground="?attr/selectableItemBackground">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="16dp">

        <!-- Primera fila: título + icono de menú -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvTituloCita"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Paseo al atardecer"
                android:textSize="18sp"
                android:textStyle="bold"
                android:textColor="#332523"
                android:maxLines="1"
                android:ellipsize="end" />

            <ImageView
                android:id="@+id/ivMenuCita"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:src="@drawable/ic_more_vert" 
                android:contentDescription="Opciones de la cita"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:clickable="true"
                android:focusable="true"
                app:tint="?attr/colorControlNormal" />

        </LinearLayout>

        <!-- Descripción -->
        <TextView
            android:id="@+id/tvDescripcionCita"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="Plan tranquilo para dar una vuelta, tomar algo y desconectar."
            android:textSize="14sp"
            android:textColor="#6A5953"
            android:maxLines="3"
            android:ellipsize="end" />

        <!-- Chips de info -->
        <LinearLayout
            android:id="@+id/layoutChips"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="10dp"
            android:orientation="horizontal"
            android:gravity="start">

            <!-- Dinero -->
            <TextView
                android:id="@+id/tvChipDinero"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:text="€ Bajo"
                android:textSize="12sp"
                android:textColor="#FF7A4D"
                android:background="@drawable/bg_chip_soft" />

            <!-- Intensidad -->
            <TextView
                android:id="@+id/tvChipIntensidad"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:text="Tranqui"
                android:textSize="12sp"
                android:textColor="#725BFF"
                android:background="@drawable/bg_chip_soft" />

            <!-- Cercanía -->
            <TextView
                android:id="@+id/tvChipCercania"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:text="Cerca"
                android:textSize="12sp"
                android:textColor="#2E8B57"
                android:background="@drawable/bg_chip_soft" />

            <!-- Temporada -->
            <TextView
                android:id="@+id/tvChipTemporada"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="6dp"
                android:paddingHorizontal="10dp"
                android:paddingVertical="4dp"
                android:text="Verano"
                android:textSize="12sp"
                android:textColor="#A0522D"
                android:background="@drawable/bg_chip_soft" />

        </LinearLayout>

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
